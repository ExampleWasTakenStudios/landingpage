name: PR

on:
  pull_request:
    types:
      - opened
      - edited
      - reopened
      - synchronize
      - converted_to_draft
      - ready_for_review
      - review_requested

jobs:
  semantics:
    name: Semantics
    uses: './.github/workflows/pr-semantics.yml'
  inital_comment:
    name: Post Initial Comment
    if: ${{ github.event.action == 'opened' && github.event.pull_request.draft == false }}
    uses: './.github/workflows/pr-initial-comment.yml'
  test:
    name: Test
    if: ${{ github.event.pull_request.draft == false && github.event.action == 'opened' || github.event.action == 'reopened' || github.event.action == 'synchronize' || github.event.action == 'ready_for_review' }}
    uses: './.github/workflows/pr-test.yml'
  deploy:
    name: Deploy
    needs:
      - test
    uses: './.github/workflows/pr-deploy.yml'
